{"cycle":"001-novelty-reserve","capability":"forward-object novelty reserve is a calibrated, contextual, Born-rule probability of novelty","claim":"pNext.reserve = P(next arrival is novel) is calibrated and contextual","stimulus_shape":"steady-novelty vs cast-then-recurrence vs regime-shift (control); + omnimodal melodies","verdict":"CONFIRMED (gap under live engine; fixed); omnimodal-confirmed (text+music)","mechanism":"live reserve = novelty/(sum+novelty) with a fixed constant -> decays with step-count, blind to novelty rate. Fix: bornNoveltyReserve = gamma-weighted recent rate of becoming-something against the recurrence void (EXACT void: basis-novelty is set-membership, not a noisy score, so the Born quantile is replaced by an exact 0-void; alpha is the only policy, floor/cap [alpha,1-alpha])","fix_layer":"interior (src/core/surprise.js bornNoveltyReserve + reserveMassFor; wired in src/perceiver/reading.js behind opts.calibrateReserve, default byte-identical)","scope":"holds: text + music, separation Δ>0.5 text / >0.88 music, control falls, forward novelty log-loss 0.792->0.638. does-not-cover: deriveNull quantile path for NOISY novelty magnitudes (left for a continuous-surprise basis); the wiring is opt-in (RULES_REV promotion deferred)"}
