// surfer — the SURFER faculty, the middle of the cognition triad (add-on 2 §A).
//
// Structure · navigate/find. The surfer is the relating function: it moves through
// the field the reader constituted and finds the relations that bear, between the
// reader (Existence · constitute) and the talker (Significance · judge/speak). It
// is the MIDDLE by construction, not by arrangement — relating is what sits between
// bringing-into-being and committing-to-surface. Its own three axes (focus, cursor,
// frame) are themselves an Existence, a Structure, and a Significance: the triad
// goes all the way down.
//
// Extracted from the fused read/ holon: the surfer (navigate) and the reader
// (constitute) were intertwined there. They remain peer faculties that import each
// other (the surfer rides the reader's reading; the reader reads forward surprise),
// but each is now its own subassembly.

export { surfFold } from './surf.js';
export { surpriseAt, forwardDist, NOVELTY_RESERVE } from './surprise.js';
export { persistentFigures, coherentFigures, motionReading, detectMotion } from './motion.js';
export * from './sequence.js';
